[phases.setup]
nixPkgs = ['python311', 'pip']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = [
  'python -c "from app.core.database import create_tables; create_tables(); print(\"Database initialized\")"'
]

[start]
cmd = 'uvicorn app.main:app --host 0.0.0.0 --port $PORT'
