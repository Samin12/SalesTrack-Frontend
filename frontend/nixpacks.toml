[phases.setup]
nixPkgs = ['nodejs_18', 'npm']

[phases.install]
cmds = ['rm -rf node_modules/.cache || true', 'npm install --no-optional']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'next start -p $PORT'
